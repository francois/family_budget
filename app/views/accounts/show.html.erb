<div class="inline-nav">
  <%= link_to("&laquo; #{@previous_account.name}", @previous_account, :class => "prev") if @previous_account %>
  <%= link_to("#{@next_account.name} &raquo;", @next_account, :class => "next") if @next_account %>
</div>
<h1><%=h @account.name %></h1>
<p><%= link_to("Modifier les détails", edit_account_url(@account)) %></p>
<%= account_spending_history(@account, @dates) if @account.expense_or_income? %>

<%= will_paginate(@transfers) %>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Détails</th>
      <th>Montant</th>
    </tr>
  </thead>
  <tbody>
    <%= render :partial => "transfer", :collection => @transfers %>
  </tbody>
</table>

<%= will_paginate(@transfers) %>
