<h1>New family</h1>

<%= error_messages_for :family %>

<% form_for(@family) do |f| %>
  <%= render :partial => "form" %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', families_path %>
