$("#<%= dom_id(bank_transaction) %>").addClass("error");
$("#<%= dom_id(bank_transaction) %> .errorText").html(<%= transfer.errors.full_messages.join("; ").to_json %>);
$("#flash").html(<%= render_flash_messages.to_json %>)
