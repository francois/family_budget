$("#<%= dom_id(bank_transaction) %>").removeClass("processed");
$("#<%= dom_id(bank_transaction) %> input.process").show();
$("#<%= dom_id(bank_transaction) %> input.transfer_id").val(null);
$("#<%= dom_id(bank_transaction) %> input.cancel").hide();
